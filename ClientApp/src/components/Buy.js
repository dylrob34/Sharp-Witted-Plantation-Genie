import React from 'react';

export class Buy extends React.Component {
    static displayName = Buy.name;

    render() {
        return (
            <div>
                <h1>This will be the page that lets a user "buy" things</h1>
                <p>Doesn't have to actually let u pay for things but looks like a page that would do that</p>
            </div>
        );
    }
}